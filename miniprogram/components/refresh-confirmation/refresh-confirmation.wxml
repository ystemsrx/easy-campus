<view
  wx:if="{{mounted}}"
  class="refresh-confirmation refresh-confirmation--{{theme}} refresh-confirmation--{{visualTheme}} {{visible ? 'refresh-confirmation--visible' : ''}} {{reducedMotion ? 'refresh-confirmation--reduced-motion' : ''}}"
  aria-role="status"
>
  <text>{{message}}</text>
</view>
