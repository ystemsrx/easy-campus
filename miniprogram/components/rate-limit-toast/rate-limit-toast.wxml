<view
  wx:if="{{mounted}}"
  class="rate-limit-toast {{reducedMotion ? 'rate-limit-toast--reduced-motion' : ''}} rate-limit-toast--{{theme}} rate-limit-toast--{{visualTheme}} {{visible ? 'rate-limit-toast--visible' : ''}}"
  aria-role="status"
>
  <text>{{message}}</text>
</view>
