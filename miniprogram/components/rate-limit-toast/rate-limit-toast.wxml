<view
  wx:if="{{mounted}}"
  class="rate-limit-toast rate-limit-toast--{{theme}} rate-limit-toast--{{visualTheme}} {{visible ? 'rate-limit-toast--visible' : ''}}"
  aria-role="status"
>
  <text>{{message}}</text>
</view>
